import { BackendInterceptor } from './backend-interceptor';

describe('BackendInterceptor', () => {
  it('should create an instance', () => {
    expect(new BackendInterceptor()).toBeTruthy();
  });
});
